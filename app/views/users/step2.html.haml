.registration
  .registration_box
    %h1.registration_box_hukidasi
      ここに登録情報を入力してください
    %h2.registration_box_contender
      .registration_box_contender_form
        = form_with model:@user, url: registration_step2_users_path(@user), method: :post, local: true do |f|
          .registration_box_contender_form_each
            %label.registration_box_contender_form_each_like_sports
              好きなスポーツはなんですか？
            %li.member-body__main__box__inner__contents__form-box__hissu
              必須
            %br
            = f.collection_select :favorite, Favorite.all, :id, :name, {prompt: "--"}, {class: "select-wrap__holder"}
          .registration_box_contender_form_each
            %label.registration_box_contender_form_each_prefecture
              出身地はどこですか？
            %li.member-body__main__box__inner__contents__form-box__hissu
              必須
            %br
            = f.collection_select :prefecture, Prefecture.all, :id, :name, {prompt: "--"}, {class: "select-wrap__holder"}
          .registration_box_contender_form
            = f.submit "次の画面へ" 
            